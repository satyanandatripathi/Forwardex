# Permission Rationale

Forwardex requests telecom and background permissions solely for local rule processing and user-configured actions.
No messages are uploaded by default.

Detailed mapping is maintained in `PermissionRationale.kt` and displayed in-app during onboarding.
